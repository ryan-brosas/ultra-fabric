[
  {
    "task": "can we review the agent utilization, I don't see we using it in away main session or planning phase so? should we remove it completely since there is no freaking use",
    "checklist": {
      "items": [
        {
          "task": "Remove PREWALK_DELEGATE_DISCIPLINE, withDelegation, the unconditional support-roles paragraph, and every delegateContext param/propagation from src/prewalk/handoff.ts",
          "validation": "grep delegateContext src/prewalk/handoff.ts returns 0 hits; armed and continuation prompts no longer contain 'consult.run' or 'Support roles' prose"
        },
        {
          "task": "Remove the delegateContext field from src/config.ts (interface, DEFAULT_FABRIC_CONFIG, normalize) and its propagation in src/prewalk/arm.ts, rearm.ts, controller.ts, lifecycle.ts",
          "validation": "grep delegateContext src/ (excluding dist) returns 0 hits; tsc --noEmit exit 0"
        },
        {
          "task": "Rewrite the two delegation sentences in the src/init/scaffold.ts AGENTS.md template (Agent surfaces bullet and Fabric behavior bullet) to describe autoScout opt-in and role agents without the delegateContext lever",
          "validation": "updated tests/fabric-init.test.ts:48 expectation passes; template no longer mentions delegateContext"
        },
        {
          "task": "Update tests: delete tests/prewalk-delegation.test.ts (tests a removed lever; port any generic armed-prompt assertions into prewalk tests first), fix tests/config.test.ts and tests/fabric-init.test.ts expectations",
          "validation": "npx vitest run tests/config.test.ts tests/fabric-init.test.ts and the prewalk suites pass"
        },
        {
          "task": "Update docs and config: AGENTS.md invariant line 9, docs/agents.md:147, docs/configuration.md:161 (also fix its stale 'autoScout defaults to true' claim), docs/ultra-fabric.md:43/319/383, and drop the delegateContext key from .pi/fabric.json",
          "validation": "grep -rn delegateContext across the repo excluding CHANGELOG.md, dist/, and git history returns 0 hits"
        },
        {
          "task": "Run the repository gate stage by stage",
          "validation": "tsc --noEmit, fresh build, full vitest suite, knip, package smoke all exit 0; git diff --check clean"
        }
      ],
      "readyAt": 1786137363460,
      "doneIndexes": [
        0,
        3,
        4
      ]
    },
    "recordedAt": 1786138359195
  },
  {
    "task": "yes commit, push, and release",
    "checklist": {
      "items": [
        {
          "task": "Commit the widget fix (src/index.ts, src/prewalk/checklist-progress.ts, tests/prewalk-progress.test.ts) as feat(prewalk), then .pi/fabric/prewalk-checklists.json as chore(prewalk): record checklist memory state",
          "validation": "git log shows both commits; git status has no staged leftovers beyond untracked root context files"
        },
        {
          "task": "Mark 0.31.1-ultra.18 per the release skill: bump package.json, add the CHANGELOG section, update both README install pins, commit exactly those three files, tag annotated v0.31.1-ultra.18, push main then the tag by explicit ref",
          "validation": "release commit touches exactly 3 files; CI publish succeeds; npm dist-tags show next: 0.31.1-ultra.18 with trustedPublisher.id == github"
        }
      ],
      "readyAt": 1786136760148,
      "easy": true,
      "doneIndexes": [
        0,
        1
      ]
    },
    "recordedAt": 1786137090518
  },
  {
    "task": "can we make our prewalk checklist UX better like when we are over doing the task we should clear the list on the screen? '/tmp/localterm-paste/48ff91bd-9c51-4bd5-ad9a-9245ef9bafa0/pasted-1786136187058-1eaae72e.png'",
    "checklist": {
      "items": [
        {
          "task": "Add a pure checklistWidgetView helper in src/prewalk/checklist-progress.ts that returns {status, lines} while items remain pending and null once every item is done (or the list is empty), with RED-first tests in tests/prewalk-progress.test.ts",
          "validation": "new tests fail before the helper exists, pass after; existing progress tests stay green"
        },
        {
          "task": "Use the helper in the src/index.ts message handler so a completed checklist tears down the fabric-prewalk-progress widget and status exactly like the idle branch, instead of retaining struck-through rows",
          "validation": "tsc, fresh build, full vitest suite, knip, package smoke all green; git diff --check clean"
        }
      ],
      "readyAt": 1786136281108,
      "easy": true,
      "doneIndexes": [
        0,
        1
      ]
    },
    "recordedAt": 1786136608726
  },
  {
    "task": "commit, push and publish it please",
    "checklist": {
      "items": [
        {
          "task": "Mark 0.31.1-ultra.17: bump package.json, add the CHANGELOG section, update both README install pins, commit exactly those three files as chore(release): mark 0.31.1-ultra.17",
          "validation": "git show --stat HEAD lists only package.json, CHANGELOG.md, README.md; grep finds ultra.17 in all three"
        },
        {
          "task": "Tag and push per the release skill: annotated tag v0.31.1-ultra.17, push main, push the tag by explicit ref (never --follow-tags)",
          "validation": "git push origin main and git push origin refs/tags/v0.31.1-ultra.17 succeed; the npm registry dist-tags show next: 0.31.1-ultra.17 once CI publishes"
        }
      ],
      "readyAt": 1786135802314,
      "easy": true,
      "doneIndexes": [
        0,
        1
      ]
    },
    "recordedAt": 1786136120526
  },
  {
    "task": "yes this is all good but make it default, always",
    "checklist": {
      "items": [
        {
          "task": "Extend InitAnswers with users/success and render them in project.md's 'Users and success' section (src/init/scaffold.ts)",
          "validation": "fabric-init.test.ts answers test asserts 'Primary users: End users' and 'Success priority: Stability' — observe RED, then GREEN"
        },
        {
          "task": "Greenfield intake asks primary users and success priority via context.ui.select when available, after the existing name/purpose inputs (src/commands/fabric.ts askGreenfield)",
          "validation": "fabric-command.test.ts greenfield test with a select mock sees both selects called and answers threaded into AGENTS.md/project.md; contexts without select still scaffold (existing tests stay green)"
        },
        {
          "task": "Preview-confirm gate: show the plan summary, then ui.select 'Write all / Cancel' before any write; Cancel aborts with nothing written",
          "validation": "new test asserts no files exist after Cancel; non-interactive contexts (no select) write as before"
        },
        {
          "task": "tech-stack.md regeneration: when the file exists and ui.confirm accepts, drop it from existingPaths so planInit recreates it from fresh detection; decline or non-interactive leaves it untouched",
          "validation": "two tests: confirm=true overwrites with detected content, confirm=false leaves original bytes"
        },
        {
          "task": "Append next-step guidance to the init notify output (prewalk / describe what to build)",
          "validation": "test asserts notify message contains '/fabric prewalk'"
        },
        {
          "task": "Default deep pass: runInit gains the state dep; on brownfield (languages detected) with scoutBridge(state.agents) available, run one explorer (maxTokens 65_536, timeoutMs 180_000) and replace the architecture placeholders in files created this run (AGENTS.md, project.md); absent agents surface notes 'deep analysis skipped' in output",
          "validation": "test with a fake spawn/wait surface sees the placeholder replaced by the brief and the run reported; test without agents sees skip note; created-this-run guard keeps pre-existing files untouched"
        },
        {
          "task": "Run the repository gate stage by stage",
          "validation": "tsc --noEmit, fresh build, full vitest suite, knip, package smoke all exit 0; git diff --check clean"
        }
      ],
      "readyAt": 1786134224535,
      "doneIndexes": [
        0,
        1,
        2,
        3,
        4,
        5
      ]
    },
    "recordedAt": 1786135216700
  },
  {
    "task": "commit, push, and publish",
    "checklist": {
      "items": [
        {
          "task": "Split the remaining working-tree changes into clean commits: feat(prewalk) research enrichment (handoff.ts enrichment hunk, arXiv test hunks, .pi/fabric.json), fix(prewalk) DONE-marker strikethrough (continuation.ts, index.ts, remaining handoff/test hunks, progress tests), chore for the checklist state file",
          "validation": "git log --oneline shows the split commits; git status clean of src/tests changes; each commit's diff contains only its concern"
        },
        {
          "task": "Release per .pi/skills/release/SKILL.md: bump package.json to 0.31.1-ultra.16, add CHANGELOG.md section, update README.md install pins, commit chore(release), annotated tag v0.31.1-ultra.16",
          "validation": "git show --stat HEAD lists exactly package.json CHANGELOG.md README.md; git tag -l points at the release commit"
        },
        {
          "task": "Push main and the release tag by explicit ref (never --follow-tags), then verify the npm publish workflow result",
          "validation": "git push origin main and git push origin refs/tags/v0.31.1-ultra.16 succeed; registry dist-tags show next -> 0.31.1-ultra.16 once CI publishes"
        }
      ],
      "readyAt": 1786131056040,
      "easy": true
    },
    "recordedAt": 1786131560645
  },
  {
    "task": "I still see clutter on on our pi... also prewalk checklist isn't crossing out done task.... I thought we fuycking implemented that",
    "checklist": {
      "items": [
        {
          "task": "RED: extend tests/prewalk-progress.test.ts (or continuation tests) to assert prewalkChecklistReminder keeps original 1-based item numbers for open items and includes an explicit [DONE:n] emission instruction",
          "validation": "npx vitest run tests/prewalk-progress.test.ts fails on the new assertions before the implementation change"
        },
        {
          "task": "GREEN: in src/prewalk/continuation.ts, change prewalkChecklistReminder to number open items by original position (index+1, not openIndex+1) and append one sentence instructing the executor to emit [DONE:n] in turn text when item n completes",
          "validation": "the new reminder tests pass; existing continuation/handoff tests still pass"
        },
        {
          "task": "Add the same [DONE:n] emission instruction to the post-acceptance executor prompts in src/prewalk/handoff.ts (PREWALK_CONTINUE_PROMPT and the checklist-accepted handoff text), with a matching assertion in tests/prewalk-handoff.test.ts",
          "validation": "npx vitest run tests/prewalk-handoff.test.ts passes with the new prompt assertion"
        },
        {
          "task": "Fix src/index.ts progress block: after markChecklistDone, re-read state.prewalk.status() before computing checklistProgress and checklistWidgetLines so the freshly marked items render struck-through in the same turn",
          "validation": "code review of the block plus vitest run of any index/progress wiring tests; strikethrough bytes \\u001b[9;2m appear for a marked item when driven through the transform"
        },
        {
          "task": "Fix clutter: unconditionally clear the fabric-prewalk-progress widget and status in src/index.ts when prewalk state is idle or has no live checklist (setWidget/setStatus teardown), not only inside the lastAssistantText branch",
          "validation": "focused test or manual trace showing the widget keys are cleared when checklist settles; no stale rows persist after retirement"
        },
        {
          "task": "Rebuild and gate in stages (pnpm run check outruns the 120s tool budget): typecheck, build, full vitest, knip, smoke; keep the unrelated in-tree handoff.ts research-enrichment diff intact and out of scope",
          "validation": "all five gate stages pass; git diff --check clean; only the planned files changed beyond the pre-existing working-tree edits"
        },
        {
          "task": "Live proof: after dist rebuild note that the running pi session needs a restart to load the new extension; verify via a fresh prewalk that [DONE:n] markers strike items through and the widget clears on settle",
          "validation": "runtime evidence per the prewalk evidence locations shows doneIndexes populated and widget teardown observed"
        }
      ],
      "readyAt": 1786115588276,
      "doneIndexes": [
        1
      ]
    },
    "recordedAt": 1786116358266
  },
  {
    "task": "can we look on our cgc or code graph mcp to look for gaps, dead code and well stuff that we can improve",
    "checklist": {
      "items": [
        {
          "task": "Break the symbols.ts/calls.ts cycle: remove the calls.js import and buildAllEdges from src/codemap/symbols.ts, export buildInheritanceEdges, and re-home buildAllEdges in src/codemap/build.ts importing edge builders from symbols.js and extractCallEdges from calls.js.",
          "validation": "grep shows no calls.js import in symbols.ts; knip --cycles reports zero circular dependencies."
        },
        {
          "task": "Update the buildAllEdges import sites: src callers plus tests/codemap-disclose.test.ts, codemap-polyglot.test.ts, codemap-cascade.test.ts now import from build.js; adjust the disclose test's entity key buildAllEdges:src/codemap/symbols.ts to build.ts.",
          "validation": "grep finds no remaining buildAllEdges import from symbols.js; codemap test modules pass."
        },
        {
          "task": "Run typecheck and the full codemap test suite, then git diff --check and git status.",
          "validation": "tsc exit 0; all codemap tests pass; diff check clean with only the named files changed."
        }
      ],
      "readyAt": 1786113580560,
      "easy": true
    },
    "recordedAt": 1786113924081
  },
  {
    "task": "can fix token optimization on this as well? like there is no dripping? maximizing all token... like every drop look for arxiv article and do clone github repo that is aligned with it....",
    "checklist": {
      "items": [
        {
          "task": "Add a RESEARCH_ENRICHMENT guidance block in src/prewalk/handoff.ts and join it into researchArmedPrompt: spend the research budget in full deep passes (no dripping small queries), and for unfamiliar algorithms or external techniques, find an aligned arXiv paper and clone the aligned GitHub repo into sources/ instead of fetching isolated files.",
          "validation": "researchArmedPrompt output contains the enrichment text; trivial and continuation prompts do not; existing prompt tests still pass."
        },
        {
          "task": "Add a focused regression test in the existing prewalk handoff test module asserting the research prompt includes the full-budget and arxiv/sources-clone guidance, observing RED first.",
          "validation": "New test fails before the prompt change and passes after; run the whole handoff test module, not just the new case."
        },
        {
          "task": "Run typecheck plus the prewalk test suite, then git diff --check and git status --short --branch.",
          "validation": "All checks pass with only src/prewalk/handoff.ts and the test module changed."
        }
      ],
      "readyAt": 1786112618376,
      "easy": true
    },
    "recordedAt": 1786113161586
  },
  {
    "task": "did we fix token optimization on this as well? like there is no dripping? maximizing all token... like every drop",
    "checklist": {
      "items": [
        {
          "task": "Verify release preconditions: tree state (3 commits ahead), the .pi/fabric.json diff disposition, remote sync, npm dist-tags, and that 0.31.1-ultra.15 is absent from the registry.",
          "validation": "git status/rev-list recorded; registry shows next=0.31.1-ultra.14 and no ultra.15; the fabric.json change is classified as commit-or-exclude with its exact diff."
        },
        {
          "task": "Run the full repository gate and hygiene checks before touching release metadata.",
          "validation": "pnpm run check green (all stages), git diff --check clean."
        },
        {
          "task": "Mark the release: bump package.json to 0.31.1-ultra.15, add the dated changelog section covering the three commits, and update both README install pins.",
          "validation": "package.json version, changelog top section, and exactly two README pins agree on 0.31.1-ultra.15."
        },
        {
          "task": "Commit chore(release): mark 0.31.1-ultra.15 with exactly the three release files, then create the annotated tag v0.31.1-ultra.15.",
          "validation": "Commit contains only package.json, CHANGELOG.md, README.md; tag exists and peels to that commit; prewalk runtime memory stays uncommitted."
        },
        {
          "task": "Push main, then push the tag by explicit ref (never --follow-tags), and monitor the Publish and Test workflow runs to conclusion.",
          "validation": "origin/main equals local main; Publish npm prerelease concludes success; Test conclusions recorded including the windows job that the argv-chunking fix targets."
        },
        {
          "task": "Verify publication and update the local plugin: npm next dist-tag, trusted-publisher provenance, GitHub Release presence, then pi install npm:ultra-fabric@0.31.1-ultra.15.",
          "validation": "next=0.31.1-ultra.15 with trustedPublisher id github, GitHub Release v0.31.1-ultra.15 exists, and pi list reports npm:ultra-fabric@0.31.1-ultra.15."
        }
      ],
      "readyAt": 1786111849069
    },
    "recordedAt": 1786112416356
  },
  {
    "task": "can we buff our codemap (AST) and make it our default... almost replace grep than using grep look for stuff inspo on arxiv + github repo",
    "checklist": {
      "items": [
        {
          "task": "Finish the in-flight Windows argv-chunking fix: add chunkPaths regression tests (boundary packing, order preservation, oversized single path) plus the bounded init-probe change already in tree, and commit it as its own upstream-shaped fix.",
          "validation": "tests cover chunkPaths edges and pass; commit contains only src/codemap/{outline,literals,calls}.ts, src/commands/fabric.ts, and the new test; tree afterwards holds only buff work."
        },
        {
          "task": "Serve the regex query class: route regex-classified queries over literal texts (strings + comments, AST-node indexed) in addition to symbol names, merging with provenance and bounded limits.",
          "validation": "RED test first: a content regex like \"retire.*oversized\" returns literal hits with file:line; symbol-name regexes keep current behavior; no file-content scanning added."
        },
        {
          "task": "Add a refs operation (semnav find_callers shape, LocAgent invoke edges): for a name or name:file key, return precise call sites and import sites with file:line and enclosing symbol, from the existing ast-grep call-site extraction before aggregation.",
          "validation": "RED test first: refs on a known function returns its real call sites with correct lines and excludes the definition itself; output bounded by maxTokens."
        },
        {
          "task": "Extend literal indexing to YAML config (ast-grep --lang yaml verified live) so config-key queries resolve; JSON via inline-rule kind match if a probe succeeds, otherwise document the exclusion.",
          "validation": "A query for a real key in .pi/fabric.json or a YAML fixture returns a typed literal hit with file:line, or the JSON exclusion is recorded with the failing probe."
        },
        {
          "task": "Flip steering to codemap-first: rewrite the tool description and promptGuidelines (grep only as empty-result fallback), update the AGENTS.md navigation line, and align prewalk handoff guidance if it names grep.",
          "validation": "Guidelines no longer reserve strings/comments/config for grep; AGENTS.md navigation rule names codemap first; grep framed as fallback when codemap returns nothing."
        },
        {
          "task": "Measure and gate: replay the mined grep corpus through route() reporting served fraction before/after, rerun benchmark:codemap:grep for no regression, run the full repository gate, and commit the buff.",
          "validation": "Served fraction rises materially with the regex+refs+config classes counted; symbol benchmark stays MRR 1.00; pnpm run check green; conventional commits with docs/code-map-research.md updated."
        }
      ],
      "readyAt": 1786109445717
    },
    "recordedAt": 1786111459329
  },
  {
    "task": "'/tmp/localterm-paste/ff835a38-be49-4a5f-a766-a813d4b91062/pasted-1786108680846-4fc9cca3.png' can we improve this UI for checklist can we have something like crossing it out for better quality of life?",
    "checklist": {
      "items": [
        {
          "task": "Confirm from the screenshot and src/index.ts:646-652 how the prewalk checklist widget renders items, including the stray ANSI reset embedded in the [DONE] branch.",
          "validation": "The exact current render expression and the accidental \\u001b[0m inside \"[DONE]\" are recorded; the ANSI pass-through of string widgets is confirmed from pi docs or theme.fg usage."
        },
        {
          "task": "Add a pure checklistWidgetLines(checklist) renderer to src/prewalk/checklist-progress.ts producing \"[x] \" + strikethrough+dim task for done items and plain \"[ ] \" lines for pending.",
          "validation": "Function stays pure with no I/O; done lines equal \"[x] \\u001b[9;2m<task>\\u001b[0m\" and pending lines equal \"[ ] <task>\"."
        },
        {
          "task": "Write RED tests in tests/prewalk-progress.test.ts covering done strikethrough, pending plain lines, and the all-pending shape before implementing.",
          "validation": "Vitest shows the new describe failing before the implementation exists and passing after."
        },
        {
          "task": "Replace the inline map in src/index.ts with checklistWidgetLines, removing the \"[\\u001b[0mDONE] \" string.",
          "validation": "src/index.ts imports checklistWidgetLines from checklist-progress.js and no \"[DONE] \" widget literal remains anywhere in src/."
        },
        {
          "task": "Run the focused prewalk-progress tests plus typecheck, then the full repository gate in stages.",
          "validation": "tests/prewalk-progress.test.ts passes, tsc --noEmit is clean, and the staged pnpm check equivalents (typecheck, build, test, knip, smoke) all pass."
        },
        {
          "task": "Commit the widget improvement on main as an upstream-shaped UI fix, excluding .pi/fabric/prewalk-checklists.json.",
          "validation": "One conventional commit contains only checklist-progress.ts, index.ts, and the test; git status shows only the runtime checklist memory left dirty."
        }
      ],
      "readyAt": 1786108814267
    },
    "recordedAt": 1786109164028
  },
  {
    "task": "update our plugin? also I don't see any releases update... also test is failing",
    "checklist": {
      "items": [
        {
          "task": "Diagnose the tag-triggered Publish npm prerelease and Test workflow runs for commit 5a31df0 using GitHub API logs, then identify why npm ultra.14 and the GitHub Release are absent.",
          "validation": "Record both run conclusions, failing job/step names, and exact error excerpts; confirm origin/main and the tag still point to the intended release commit."
        },
        {
          "task": "Inspect the locally installed npm:ultra-fabric version and Pi package update command from the complete packages documentation, without updating until npm publication succeeds.",
          "validation": "Report current installed version/path and the documented update command; no local package mutation occurs before the registry exposes ultra.14."
        },
        {
          "task": "Implement the smallest release/test fix in source and workflow, with focused regressions, preserving the published release metadata and excluding .pi/fabric/prewalk-checklists.json runtime memory.",
          "validation": "Focused failing tests reproduce each workflow failure before the fix and pass afterward; no dependency or lockfile change unless the failure proves it necessary."
        },
        {
          "task": "Run pnpm run check, git diff --check, and git status --short --branch; commit and push the repair on main.",
          "validation": "All repository gates pass, the repair commit contains only intended source/workflow/tests, and origin/main equals local main."
        },
        {
          "task": "Retag v0.31.1-ultra.14 to the repair commit only if the registry confirms the version was never published, then push the explicit tag ref and monitor both workflows.",
          "validation": "Tag movement is limited to the unpublished ultra.14 tag, Publish npm prerelease and Test conclude success, and a GitHub Release exists for v0.31.1-ultra.14."
        },
        {
          "task": "Verify npm next and trusted-publisher provenance, then update the locally installed Ultra Fabric plugin to 0.31.1-ultra.14 and confirm Pi resolves that version.",
          "validation": "npm next equals 0.31.1-ultra.14, _npmUser.trustedPublisher.id is github, package update completes successfully, and pi list reports npm:ultra-fabric@0.31.1-ultra.14."
        }
      ],
      "readyAt": 1786107921829
    },
    "recordedAt": 1786108561456
  },
  {
    "task": "so what else do we need other stuff to be implemented?",
    "checklist": {
      "items": [
        {
          "task": "Re-verify the complete dirty working tree on main and partition it into coherent commits: context QoS/message-ceiling behavior, prewalk wording regression, release automation, generated prewalk checklist memory, and the final release metadata.",
          "validation": "git diff --check is clean; pnpm run check passes from Main; every dirty path is explicitly assigned or intentionally excluded, with no secrets or generated dist staged."
        },
        {
          "task": "Commit the tested context engineering implementation and docs from src/context, src/compaction, config/settings, focused tests, docs/compaction.md, and docs/ultra-fabric.md.",
          "validation": "The commit contains only the context-surge and 800-message prevention files; focused QoS/threshold/config tests remain green."
        },
        {
          "task": "Commit the prewalk delegation wording fix and its focused regression separately, excluding .pi/fabric/prewalk-checklists.json because it is runtime memory rather than release source.",
          "validation": "The commit contains only src/prewalk/handoff.ts and tests/prewalk-delegation.test.ts; the focused prewalk test passes."
        },
        {
          "task": "Commit the release workflow hardening, release-note generator, release skill guidance, and their tests as a separate release-infrastructure commit.",
          "validation": "The commit contains only .github/workflows/release.yml, .pi/skills/release/SKILL.md, scripts/release-notes.mjs, tests/release-notes.test.ts, and tests/release-workflow.test.ts; focused release tests pass."
        },
        {
          "task": "Prepare prerelease 0.31.1-ultra.14 by changing exactly package.json, CHANGELOG.md, and both README.md install pins, then commit chore(release): mark 0.31.1-ultra.14 and create annotated tag v0.31.1-ultra.14.",
          "validation": "Package version, changelog heading/date, README pins, and tag name/message agree exactly; registry confirms ultra.14 is absent before publication."
        },
        {
          "task": "Push main, push only refs/tags/v0.31.1-ultra.14, monitor the GitHub release workflow, and verify npm next plus trusted publisher provenance.",
          "validation": "origin/main equals local main; the workflow concludes success; npm dist-tag next equals 0.31.1-ultra.14 and _npmUser.trustedPublisher.id is github."
        }
      ],
      "readyAt": 1786107485309
    },
    "recordedAt": 1786107775152
  },
  {
    "task": "what happened? did we implement?",
    "checklist": {
      "items": [
        {
          "task": "Add a focused RED regression in tests/compaction-threshold.test.ts that builds 699- and 700-message active contexts at low token occupancy and proves only the configured message boundary triggers context.compact().",
          "validation": "The narrow test initially fails because FabricConfig has no messageThreshold and compactAtConfiguredThreshold currently checks token occupancy only."
        },
        {
          "task": "Extend FabricCompactionConfig in src/config.ts with a bounded messageThreshold, defaulting below the provider 800-message cap with safety room for injected/system messages, and parse older configs through the default without migration breakage.",
          "validation": "Config tests prove default, custom value, lower/upper bounds, and existing .pi/fabric.json compatibility; no dependency is added."
        },
        {
          "task": "Update src/compaction/threshold.ts to count context.sessionManager.buildSessionContext().messages and compact when either message count reaches messageThreshold or token occupancy reaches the existing per-model/global threshold.",
          "validation": "Direct tests prove 699 messages do not compact, 700 messages do compact at 10% tokens, token thresholds still behave unchanged, null token telemetry cannot suppress a message-count trigger, and compaction failure remains bounded/non-throwing."
        },
        {
          "task": "Expose the new threshold in src/ui/settings.ts and document the 800-message provider failure, proactive safety margin, exact active-context counting, and manual recovery command in docs/compaction.md and docs/ultra-fabric.md.",
          "validation": "The setting name and default match source; docs distinguish proactive prevention from upstream 413 overflow classification and state that current failed sessions should run /compact once before retrying."
        },
        {
          "task": "Assess and record the upstream-shaped pi-ai classifier gap: 0.84.1 overflow patterns do not match chat_history_too_large, payload_too_large, message_limit, or the 800-message text. Avoid dependency patching unless repository policy and tests prove it is required after proactive prevention.",
          "validation": "Evidence cites node_modules/@earendil-works/pi-ai/dist/utils/overflow.js and explains why token-pressure compaction did not auto-recover this 413; no node_modules or lockfile mutation occurs."
        },
        {
          "task": "Run the focused threshold/config/settings tests, context certification, then pnpm run check, git diff --check, and git status --short --branch, preserving unrelated dirty release/prewalk work.",
          "validation": "All checks pass, dist is generated only by build, the direct 700-message probe compacts before provider dispatch, and the final report separates this change from pre-existing working-tree modifications."
        }
      ],
      "readyAt": 1786106846590
    },
    "recordedAt": 1786107322007
  },
  {
    "task": "There is a context surge when we are stuff... from 2% then just exploded to 79.5% can we look on our cgc, fix and enforce better context engineering practices wheter we need to put this on documents or .md so we aren't\n going to explode or something",
    "checklist": {
      "items": [
        {
          "task": "Reproduce and classify the surge without mutation: inspect the active session trace and Context QoS reports, then force-refresh the ultra-fabric code graph and trace the request path through src/index.ts, src/context/qos.ts, src/context/handoff-retirement.ts, src/compaction/hook.ts, and src/execution-service.ts. Separate transcript growth, tool-result growth, repeated prewalk injection, and provider-reported token-estimate effects.",
          "validation": "Record a compact evidence table with before/after token or character counts and file:line locators. The chosen root cause must explain the observed jump from roughly 2% to 79.5%, or be explicitly labeled unconfirmed if the session telemetry is unavailable."
        },
        {
          "task": "Use the project graph and the read-only CGC inspiration graph to compare Ultra Context QoS with the documented pi-vcc precedents, focusing on proactive pressure thresholds, deterministic preservation, bounded outputs, and no-op behavior below threshold. Inspect only bounded symbol windows and return locators rather than source dumps.",
          "validation": "Document which invariants already exist, which gap permits the surge, and why any adopted behavior stays independently implemented and within docs/adr/0001-fork-boundary.md."
        },
        {
          "task": "Write focused failing tests first around the confirmed gap in tests/context-qos.test.ts and/or tests/context-handoff-retirement.test.ts, plus the nearest execution or compaction integration test. Cover one-shot oversized fabric_exec output, repeated equivalent reads, recent-turn protection, evidence/error/mutation preservation, malformed or non-text results, and no-op behavior below the configured boundary.",
          "validation": "Run the narrow test files and observe RED for the intended missing behavior without changing existing protected-message expectations."
        },
        {
          "task": "Implement the smallest host-owned enforcement in src/context/qos.ts and its actual request hook, extending src/context/handoff-retirement.ts or src/compaction/hook.ts only if the traced path proves necessary. Prefer deterministic retirement or bounded projection with typed markers over model-generated summaries, preserve tool call/result pairing, and avoid new dependencies.",
          "validation": "The focused tests turn GREEN, direct probes show a large stale result is reduced before the next model request, and recent turns, errors, writes, gates, traces, and evidence remain byte-exact."
        },
        {
          "task": "Add or adjust typed configuration in src/config.ts and its migration/default/schema tests only if enforcement needs an operator threshold. Keep defaults safe and backward compatible, bound every numeric value, and ensure automatic policy promotion remains disabled.",
          "validation": "Config parsing tests prove valid defaults, boundary clamping or rejection, migration compatibility, disabled/no-op behavior where applicable, and no accidental enabling of autoScout or policy promotion."
        },
        {
          "task": "Update docs/compaction.md and the relevant context-engineering section of docs/ultra-fabric.md with operational practices: AST/CGC-first navigation, bounded reads and tool returns, structured evidence locators, fresh-context delegation under pressure, and the exact enforcement/protection guarantees. Update AGENTS.md only if a durable contributor rule is missing rather than duplicating architecture documentation.",
          "validation": "Docs name the real config and marker fields, distinguish runtime enforcement from human guidance, explain edge cases and observability, and contain no claim unsupported by source or tests."
        },
        {
          "task": "Run proof in increasing scope: focused context tests, the context certification or smallest relevant benchmark/probe, then the repository handoff gates pnpm run check, git diff --check, and git status --short --branch. Inspect any failure rather than rerunning unchanged.",
          "validation": "Report measured before/after context size for the reproduction, all focused and repository checks pass, public symbols/config registrations are confirmed, generated dist is produced only by the build, and the final diff contains no unrelated changes."
        }
      ],
      "readyAt": 1786105322087
    },
    "recordedAt": 1786106461728
  }
]